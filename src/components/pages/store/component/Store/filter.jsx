import React, { Component } from 'react'
import { useState, useEffect } from 'react'
import $ from 'jquery'
// import SideTags from './SideTags'

export default function filter(state = 'TAG_', action) {
    switch (action.type) {
        default:
            return state
    }
}
